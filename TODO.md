# TODO

[ ] Add more higher level methods for one line control commands
[ ] Ability to pass in event handlers for 'data', 'error', and 'close' events on HID device
[ ] .fill(message: string | number[]) method to fill whole screen with a string or char code array
[ ] .fillRow() and .fillColumn() methods that do the same as above but for a specific row or column
